{% extends 'base.html.twig' %}

{% block title %}Bioramas - Modifier mon mot de passe{% endblock %}

{% block content %}
    <h1>Modifier mon mot de passe</h1>
    <a href="{{ path('account')}}">- Modifier mon mot de passe</a><br/>
    <hr>
    {% if notification %}
        <div class="alert alert-info">{{ notification }}</div>
    {% endif %}
    {{ form(form) }}
    <hr>

{% endblock %}